What happens when you initialize a repository? Why do you need to do it?

A hidden file called .git is created. You create a repo to use git

How is the staging area different from the working directory and the repository?
 What value do you think it offers?
 
 The staging area is a place to transfer the new updates from the working
 directory, just before the repository. It allows you to only keep specific
 files
 

What are the pros and cons of Git’s automatic merging vs. always doing merges 
manually?

Pros:
	manual:
	Be sure that the correct code is put in the new merged code
	
	auto:
	less hassel
cons:

	manual:
	Gotta talk to other devs to solve conflicts
	auto:
	may resolve a conflict incorrectly



